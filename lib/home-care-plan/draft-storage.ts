/**
 * @deprecated Import from `@/lib/persistence` instead.
 * Re-exports for backward compatibility — same sessionStorage keys preserved.
 */
export {
  clearGeneratedHomeCarePlan,
  loadGeneratedHomeCarePlan,
  saveGeneratedHomeCarePlan,
} from "@/lib/persistence/repository";
